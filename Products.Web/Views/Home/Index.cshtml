@model IEnumerable<ProductDto>

<form>
    <div class="container row">
        @foreach (var item in Model)
        {
            <div class="col-4">
                <div class="p-0 my-3 border">
                    <div class="card">
                        <img src="@item.ImageUrl" class="card-img-top" style="height:200px; width:200px;" alt="Alternate Text">
                        <div class="card-body">
                            <h3 class="card-title text-success">@item.Name</h3>
                            <span class="badge badge-light mb-2">@item.CategoryName</span>
                            <p class="card-text">@item.Description</p>
                            <div class="row">
                                <div class="col pt-1">
                                    <span style="font-size:23px;">@string.Format("{0:c}", @item.Price)</span>
                                </div>
                                <div class="col">
                                    <a asp-action="Details" asp-route-productId="@item.ProductId" class="btn btn-success form-control">Details</a>
                                </div>
                            </div>
                        </div>
                        
                    </div>
                </div>
            </div>
        }
    </div>
</form>

<!--<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1>Products</h1>
            <div class="col text-end pt-1">-->
                @*<a asp-action="Create" class="btn btn-primary">
                    <i class="fas fa-plus">Create New Product</i>
                </a>*@

            <!--</div>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Category Name</th>
                <th>Prrice</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>

                        @item.Name
                    </td>
                    <td>
                        @item.CategoryName
                    </td>
                    <td>
                        @(item.Price.ToString("c"))
                    </td>
                    <td>
                        <a asp-action="Details" asp-route-productId="@item.ProductId" class="btn btn-success">
                            <i class="fas fa-edit"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>

    </table>-->
@*</div>*@ 